using Csharp15;


/// Sum elements in the same index of two array sequences, if one sequence is shorter, the missing elements are replaced with default value of the element type (0 for int).

BenchmarkDotNet.Running.BenchmarkRunner.Run<MyBenchMarks>();